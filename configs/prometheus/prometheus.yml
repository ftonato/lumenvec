global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: lumenvec
    static_configs:
      - targets:
          - lumenvec:19190
    metrics_path: /metrics
